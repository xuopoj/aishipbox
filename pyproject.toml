[project]
name = "aishipbox"
version = "0.1.4"
description = "ModelArts Studio 自定义数据加工算子开发 CLI"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
dependencies = [
    "questionary>=2.0.0",
    "pyyaml>=6.0",
    "debugpy>=1.8.0",
    "tomli>=2.0.0; python_version < '3.11'",
]

[project.urls]
Homepage = "https://github.com/xuopoj/aishipbox"
Repository = "https://github.com/xuopoj/aishipbox"
Issues = "https://github.com/xuopoj/aishipbox/issues"

[project.scripts]
aishipbox = "aishipbox.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["aishipbox"]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-mock>=3.10",
]

[tool.pytest.ini_options]
markers = ["integration: end-to-end flows that touch venv and uv"]
