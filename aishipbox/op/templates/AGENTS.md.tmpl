# AGENTS.md

ModelArts Studio 自定义数据加工算子项目 — AI agent 快速上手与约束。

- 工具：aishipbox v${aishipbox_version}
- 托管运行时：Python 3.10（已在 `.venv/` 中）
- 入口：`program_package/process.py` 中的 `Process` 类（必填，实现 `__call__`）

## 文件布局

```
.
├── manifest.yml                      # 算子元数据（id/name/modal/runtime/arguments/labels）
├── program_package/
│   ├── process.py                    # 算子主实现（必填）
│   ├── dependency/
│   │   └── requirements.txt          # 额外依赖（平台 ~250 个常用包已预置）
│   └── install.sh.example            # 自定义安装命令模板
├── obs_input/                        # 本地 mock 输入（→ obs://input/）
├── obs_output/                       # 本地 mock 输出（→ obs://output/）
├── .env.example  .env                # `--obs` 模式凭据（OBS_AK/SK/ENDPOINT/...）
├── .vscode/launch.json               # `aishipbox op debug` 生成
├── .venv/                            # 项目独立的 Python 3.10 venv
└── .aishipbox.toml                   # 项目类型标记（勿改 schema_version / type）
```

## process.py 两种模式

由 `manifest.yml > runtime > auto-data-loading` 决定（先 `cat manifest.yml` 确认当前模式）。

### 模式一 — auto-data-loading: true

框架按文件类型构造 pandas.DataFrame，**逐文件**调用 PreProcess → Process → PostProcess。

```python
import pandas as pd
import ma_utils as utils

logger = utils.FileLogger.get_logger()


class PreProcess:                        # 可选
    def __init__(self, args): ...
    def __call__(self, input: pd.DataFrame) -> pd.DataFrame: ...


class Process:                           # 必填
    def __init__(self, args): ...
    def __call__(self, input: pd.DataFrame) -> pd.DataFrame: ...


class PostProcess:                       # 可选
    def __init__(self, args): ...
    def __call__(self, input: pd.DataFrame) -> pd.DataFrame: ...
```

规则：
- `__init__` 整次运行只调一次（适合模型加载、状态分配）
- `__call__` 每个输入文件调一次（这一行对操作语义很重要）
- 必须返回 DataFrame；返回 `None` 或其他类型会触发警告并沿用上一阶段输出
- DataFrame 形状按文件扩展名分派：

  | 扩展名 | 列 |
  |---|---|
  | `.jsonl` / `.csv` | 文件自身字段 |
  | `.parquet` | 文件自身字段 + 系统注入 `file_path` + `file_name` |
  | 其他 | 单行 `file_path` + `file_name` |

- **同一 `obs_input/` 禁止混合上述类型** —— runner 会立即报错。多模态请改用模式二
- mock 模式下，最终各文件返回的 DataFrame 会被汇总写入 `obs_output/result.jsonl`（仅本地约定，平台由框架决定输出位置）

### 模式二 — auto-data-loading: false

输入是空 DataFrame；算子自行从 `args.obs_input_path` 读、写回 `args.obs_output_path`，**无返回值**。

```python
class Process:
    def __init__(self, args):
        self.args = args

    def __call__(self, input: pd.DataFrame):
        import moxing as mox
        for f in mox.file.list_directory(self.args.obs_input_path):
            # 自行处理 OBS I/O
            ...
```

适用：多模态数据集、需要整集输入（如去重）、输出非标准模态。

## args 对象

`__init__(self, args)` 收到的 `args` 是 argparse Namespace，包含：

- `args.obs_input_path` / `args.obs_output_path` —— 框架注入的 OBS 路径
- `manifest.yml > arguments` 中声明的业务参数（按 `key` 字段挂到 `args` 上；本地用 `default` 值，平台用用户在 UI 中填写的值）

例如 manifest 里有 `arguments: [{key: threshold, type: FLOAT, default: 0.5}]`，运行时 `args.threshold == 0.5`。

## OBS 访问（moxing.file）

```python
import moxing as mox

mox.file.copy("obs://input/a.jpg", "obs://output/a.jpg")
mox.file.copy_parallel("obs://input/dir/", "obs://output/dir/")
mox.file.list_directory("obs://input/", recursive=True)
mox.file.read("obs://input/a.txt")              # 返回 str
mox.file.read("obs://input/a.bin", binary=True) # 返回 bytes
mox.file.write("obs://output/b.txt", "...")
with mox.file.File("obs://input/x", "r") as f:  # 类似 open()
    ...
# 其余：append / exists / is_directory / make_dirs / mk_dir /
#       remove / rename / glob / walk / stat / get_size / scan_dir
```

本地 mock 把 `obs://input/` 映射到 `obs_input/`、`obs://output/` 映射到 `obs_output/`。**其他 bucket 名会立即报错** —— 本地只在这两条路径上保证等价。

## 日志

用 `ma_utils.FileLogger.get_logger()`，**不要**用 stdlib `logging.basicConfig` —— 平台日志收集依赖 FileLogger 的格式与目标。本地 mock 提供等价的 logger。

```python
import ma_utils as utils
logger = utils.FileLogger.get_logger()
logger.info("...")
```

## 命令

子命令：`new` / `run` / `pack` / `debug` / `download`。**用法、参数、默认值以 `--help` 为准**（自带完整说明，按需查）：

```bash
aishipbox op --help              # 子命令总览
aishipbox op <cmd> --help        # 某条命令的参数与默认值，如 aishipbox op run --help
```

常用：`op run`（本地 mock，默认）、`op run --obs`（真实 OBS，先填 .env）、`op run --debug` + `op debug`（VS Code 调试）、`op download <pkg>`（加依赖）、`op pack`（打包）。

- **非交互环境必须带 `--yes`** 跑 `op new`（无 TTY 时不带会报错，不会卡向导）。
- **修改 `manifest.yml` 或依赖后必须重新 `op pack`**（pack 自带 manifest 校验）。

## 依赖

平台基础镜像预装约 250 个常用包（pandas/numpy/torch/transformers/datasets/pyarrow/opencv-python/pillow/moxing-framework 等）。

**`program_package/dependency/requirements.txt` 只列额外依赖** —— 这个文件随 `op pack` 打入算子包，平台运行时执行：

```bash
pip install --no-index --find-links=./dependency -r ./dependency/requirements.txt
```

`--no-index` 意味着 pip 只能从 `./dependency/` 找 wheel，不能上 PyPI。所以：
- **不要**把已预置的包（pandas/numpy/pyarrow/...）写进 `requirements.txt`
- 平台版本如果漂移，pip 找不到 wheel 就会断
- 平台预置版本由 `aishipbox op new` 在本地自动安装到 `.venv/`（见 aishipbox 仓库 `core/config.py` 的 `PLATFORM_PRESET_PINS`），保证本地 = 平台等价

要新加平台没有的依赖，推荐用 `aishipbox op download <package>`：
1. 按 `manifest.yml > runtime > cpu-arch` 解析出的架构（`ARM`→`manylinux2014_aarch64`，`X86`→`manylinux2014_x86_64`），下载对应 Linux + Python 3.10 wheel 到 `dependency/`（**不下载依赖的依赖**，即 `--no-deps`，避免传递依赖把已预置包带进包里）
2. 自动把解析出的 `<package>==<version>` 写入/更新 `requirements.txt`
3. 重新 `op pack`

也可以手动操作：把 `.whl` 放进 `dependency/`，对应行写进 `requirements.txt`，或在 `install.sh` 里写更复杂的安装步骤。

## 关键约束

- 算子通过 `moxing` / `ma_utils` 访问平台基础设施；本地 mock 与平台等价
- 修改 `manifest.yml` 后请重新 `pack`
- 不要修改 `.aishipbox.toml` 的 `schema_version` 与 `type` 字段
- 不要把平台预置包（pandas/numpy/pyarrow/...）写进 `requirements.txt` —— 会让平台 pip 失败

## manifest.yml 完整字段规范

以下字段由 aishipbox 在 `aishipbox op pack` 前用同一套规则校验（见 aishipbox 仓库 `op/manifest.py`），不符合规范会直接报错并列出全部问题。

### 顶层字段

| 字段 | 必填 | 规则 |
|---|---|---|
| `id` | 是 | 英文字母开头，仅含英文字母/数字/下划线；≤128 字符；创建后不可变 |
| `name` | 是 | 显示名；≤128 字符 |
| `description` | 否 | ≤2000 字符 |
| `author` | 否 | ≤128 字符 |
| `version` | 是 | 必须是 `x.y.z` 格式 |
| `tags.language` | 是 | ISO 639-1 代码数组，如 `zh` / `en` |
| `tags.format` | 否 | 文件格式数组：`JSONL` / `TXT` / `CSV` / `HTML` / `MP4` / `AVI` / `DOCX` / `PDF` / `EPUB` / `MOBI` / `BACKFLOW` 等 |
| `tags.category` | 是 | 单选，仅限：`数据提取` / `数据抽样` / `数据转换` / `数据过滤` / `数据去重` / `数据打标` / `其他` |
| `tags.modal` | 是 | 数组，仅限：`TEXT` / `IMAGE` / `VIDEO` / `AUDIO` / `OTHER` |
| `tags.custom` | 否 | 自定义标签数组；每项 ≤32 字符 |

### runtime

| 字段 | 必填 | 规则 |
|---|---|---|
| `runtime.cpu-arch` | 是 | 数组，仅支持 `ARM` / `X86` |
| `runtime.xpu-devices` | 条件必填 | 任一 `resources[].npu > 0` 时必填，仅支持 `SNT9B` |
| `runtime.environment` | 固定 | `python`（由 aishipbox 生成，不要手改） |
| `runtime.entrypoint` | 固定 | `process.py`（由 aishipbox 生成，不要手改） |
| `runtime.auto-data-loading` | 是 | `true`/`false`，见上文两种模式 |
| `runtime.resources` | 是 | 至少一项；每项 `cpu` ≥1、`memory` ≥1（MB）、`npu` ≥0（默认 0） |

### arguments（业务参数，可选数组）

| 字段 | 必填 | 规则 |
|---|---|---|
| `key` | 是 | 算子内唯一；≤128 字符；运行时挂到 `args.<key>` |
| `name` | 是 | 显示名 |
| `type` | 是 | `STRING` / `FLOAT` / `INT` / `ENUM` / `LIST` / `OBS` / `BOOLEAN` / `DYNAMIC_INPUT_LIST` |
| `tips` | 否 | 帮助文本；≤2000 字符 |
| `between` | `INT`/`FLOAT` 必填 | 是否为范围参数（true/false），必须显式声明 |
| `min` / `max` | 条件 | 数值类型的边界，必须是数字 |
| `items` | `ENUM`/`LIST`/`BOOLEAN` 必填 | 至少一项，每项含 `name` 和 `value` |
| `required` | 否 | 默认 false |
| `visible` | 否 | 前端是否展示；默认 false |
| `default` | 条件 | `visible=false` 且 `required=true` 时必填；`between=true` 时格式为 `"min;max"` |

类型示例：

```yaml
# 数值范围（between=true）
- key: clip_duration
  name: 片段时长范围
  type: FLOAT
  between: true
  min: 0.1
  max: 600.0
  default: "1.0;300.0"   # "min;max"

# 单选枚举
- key: font_conversion
  name: 字体转换
  type: ENUM
  between: false
  items:
    - {name: 简体, value: simplified}
    - {name: 繁体, value: traditional}
  default: simplified

# 多选列表
- key: resolution
  name: 分辨率
  type: LIST
  items:
    - {name: SD, value: SD}
    - {name: HD, value: HD}
  default: "SD,HD"        # 逗号分隔
```

运行时 `args.<key>` 取值：本地 mock 用 manifest 里的 `default`；平台用用户在 UI 里实际填写的值。

### labels（输出打标字段，可选数组，仅打标类算子需要）

| 字段 | 必填 | 规则 |
|---|---|---|
| `key` / `name` | 是 | 标签标识 / 显示名 |
| `type` | 是 | `STRING` / `NUMERIC` / `ENUM` / `OBJECT` |
| `min` / `max` | `NUMERIC` 必填 | 数值范围 |
| `items` | `ENUM` 必填 | 至少一项，每项含 `name` + `value` |
| `dimensions` | `OBJECT` 必填 | 至少一项；每项含 `key` + `name` + `type`（`STRING`/`NUMERIC`/`ENUM`），`NUMERIC` 需 `min`/`max`，`ENUM` 需 `items` |

```yaml
labels:
  - key: quality_score
    name: 质量分
    type: NUMERIC
    min: 0.0
    max: 100.0
  - key: detailed_analysis
    name: 详细分析
    type: OBJECT
    dimensions:
      - {key: sentiment, name: 情感, type: ENUM, items: [{name: 正面, value: positive}]}
      - {key: confidence, name: 置信度, type: NUMERIC, min: 0.0, max: 1.0}
```

修改 `arguments` / `labels` / `runtime` 后，跑一次 `aishipbox op pack` 即可看到 manifest 校验报错（如果有）—— 不需要单独跑 lint。

## 平台参考

- 算子开发手册：https://support.huaweicloud.com/usermanual-pangulm/pangulm_04_0043.html
- 算子配置文件规范：https://support.huaweicloud.com/usermanual-pangulm/pangulm_04_0044.html
- MoXing API：https://support.huaweicloud.com/usermanual-standard-modelarts/modelarts_11_0001.html
