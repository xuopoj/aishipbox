# 算子运行所需依赖（仅列出未预置的包；平台基础镜像已预装 pandas / numpy / torch /
# moxing-framework / opencv-python / PyYAML 等约 250 个常用包）。
#
# 若依赖无法从 PyPI 获取，可将 .whl 放在本目录（program_package/dependency/），
# 平台运行时会执行：
#   pip install --no-index --find-links=./dependency -r ./dependency/requirements.txt
#
# 平台运行时已预置；这里列出仅为本地虚拟环境可运行 process.py。
# 版本严格对齐平台基础镜像，避免 pandas 2.x / numpy 2.x 等 API 差异导致
# 本地通过但平台运行失败。
pandas==1.3.5
numpy==1.26.4
pyarrow==18.0.0

# 示例（额外依赖）：
# pillow==10.4.0
